#//// Neoffice — added file (no upstream equivalent). openGym is a plain web app
#//// upstream; on Neoffice it also has to be a Frappe app, so that Frappe serves
#//// its built assets from /assets/opengym/. Nothing else about the project
#//// changes: no doctypes, no server code, no hooks beyond the app identity.
__version__ = "1.2.4"
